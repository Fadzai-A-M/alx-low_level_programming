#include "main.h"

/**
 * int _islower  -  Writes a function that checks for lowercase character
 *
 *@c: the character to check
 *
 * Return: 1 if lower case (Success)
 */
int _islower(int c);

{
	if (islower(c))

	{
		return (1);
	}

	else

	{
		return (0);

	}
}
